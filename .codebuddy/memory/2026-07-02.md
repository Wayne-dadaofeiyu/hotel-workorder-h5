# 2026-07-02

## 移除 DEMO_ROOM 旧逻辑 (commit 4ef10db)
- 从 `mockData.ts` 移除：
  - `DEMO_ROOM = '0314'` 常量
  - `DEMO_GUEST_NAMES`、`DEMO_DELIVERY_ITEMS`、`DEMO_CLEANING_ITEMS` 专用数据池
  - `generateMockOrders()` 中为 0314 生成 1送物+1打扫的逻辑
  - "0314 始终排最前"的排序逻辑 → 改为纯时间倒序
- **唯一保留的演示逻辑**：`PendingListPage.tsx` 中切换到 Cleaning tab 时，自动注入一条 0314 房的即时打扫通知（isInRoom=true, specialNotes="Guest is waiting in the room"），同一 session 只触发一次
- 0314 不在随机房号池 `ROOM_NUMBERS` 中
